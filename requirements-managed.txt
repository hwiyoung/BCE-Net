# Packages installed into .venv-bcenet-geo by scripts/setup_env.sh.
#
# PyTorch/torchvision are intentionally not listed here.  The managed GPU
# container supplies CUDA-matched builds, and the venv inherits them through
# --system-site-packages.
numpy==2.2.6
pandas==2.3.3
rasterio==1.5.0
geopandas==1.1.3
pyogrio==0.12.1
shapely==2.1.2
affine==2.4.0
